This is a calculator desktop application written in Java. The calculator does basic math operations such as add, substract, multiply and divide. It can do many operations sequentially. 

## Code
The application uses Java, and builds and manages packages with Maven. The main application code is inside src/main/java/it/app. 